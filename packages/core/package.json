{
  "name": "@kounselia/core",
  "private": true,
  "version": "0.1.0",
  "description": "Network calls and data shapes shared by the web chat (chat-app/) and the mobile app (mobile/). No DOM, no React Native, no dependencies, so both can use it as-is.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
