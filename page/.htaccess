# Friendly URLs for editable pages: /page/our-mission -> page/index.php
# (the script reads the slug from the URL itself).
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L,QSA]
</IfModule>
